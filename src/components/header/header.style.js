import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
    header: {
        width: "50%",
        alignItems: "center"
    },
    logo: {
        width: 400,
        height: 400,
    },
    titulo: {
        color: COLORS.dark_gray,
        fontSize: FONT_SIZE.md,
        textAlign: "center"
    }
}